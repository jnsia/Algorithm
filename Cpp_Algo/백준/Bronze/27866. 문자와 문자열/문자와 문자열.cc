#include <iostream>
using namespace std;

int main(void) {
	char target, str[1001];
	int str_len;

	cin >> str >> str_len;

	for (int i = 0; i < str_len; i++)
	{
		target = str[i];
	}

	cout << target;
}